# TrinetLayer Cyber Labs — Environment Configuration
#
# The application runs with NO environment variables required by default.
# All labs, scoring, interactive simulation, and analytics run 100% client-side.
#
# Copy this file to .env for local development overrides if needed.
# NEVER commit sensitive keys or secrets.
